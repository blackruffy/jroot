OS := $(shell uname)
VERSION := 0.6.0
ROOT_VERSION := 6.06.00

CPP_ROOT_DIR := `root-config --prefix`
CPP_ROOT_LIBS := `root-config --libs --glibs`
CPP_ROOT_FLAGS := `root-config --cflags`

JAVA = ${JAVA_HOME}/bin/java
JAVAC = ${JAVA_HOME}/bin/javac
JAR = ${JAVA_HOME}/bin/jar
JAVADOC = ${JAVA_HOME}/bin/javadoc
